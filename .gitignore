.gemini/
node_modules/
.DS_Store
Thumbs.db
__pycache__/

# Claves privadas
secrets.js

# Backups (no versionar)
*.zip
respaldo_antes_de_whisper.zip
backup_antes_de_opus.zip

# Informes fuente (datos de pacientes — NUNCA subir a GitHub)
informes_fuente/

# Datos de evaluación (contienen informes reales — NO subir). El script sí se versiona.
eval/casos_prueba.jsonl
eval/resultados.jsonl
eval/__pycache__/

# Salidas del comparador de modelos (derivadas de informes reales — NO subir). El script sí se versiona.
eval/resultados_*.jsonl
eval/comparacion_modelos*
